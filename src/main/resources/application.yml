spring:
  application:
    name: bank-cards
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  liquibase:
    enabled: true
    change-log: classpath:db/migration/db.changelog-master.xml
server:
  port: 8080
jwt:
  lifetime: 30m
encoder:
   secret: QMULq8kR3pVbV0w9q8e5j7nC2vF4hXyD
   algoritm: AES

logging:
  level:
    org.springframework.security: trace

